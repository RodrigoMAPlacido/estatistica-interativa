schema: EconSchema
aspectRatio: 1.55

params:
  - name: p
    value: 0.5
    min: 0.01
    max: 0.99
    round: 0.001

calcs:
  # =========================
  # Medidas-resumo
  # =========================
  p0: "(1 - params.p)"
  p1: "(params.p)"
  media: "(params.p)"
  variancia: "(params.p * (1 - params.p))"
  sigma: "((calcs.variancia)^0.5)"
  somaProb: "(calcs.p0 + calcs.p1)"

colors:
  ink: "'#111827'"
  soft: "'#64748b'"
  grid: "'#d1d5db'"
  bg: "'#ffffff'"

layout:
  OneGraphPlusSidebar:
    graph:
      xAxis:
        min: -0.8
        max: 1.8
        ticks: 0
      yAxis:
        min: 0
        max: 1
        ticks: 6
        show: false
        title: "P(X=x)"

      objects:

        # =========================
        # Barras da distribuição
        # =========================
        - Rectangle:
            x1: -0.35
            x2: 0.35
            y1: 0
            y2: calcs.p0
            fill: colors.ink
            opacity: 1

        - Rectangle:
            x1: 0.65
            x2: 1.35
            y1: 0
            y2: calcs.p1
            fill: colors.ink
            opacity: 1

        # =========================
        # Linha da média
        # =========================
        - Segment:
            a: [calcs.media, 0]
            b: [calcs.media, 1.0]
            color: colors.ink
            strokeWidth: 1

        - Label:
            coordinates: [calcs.media, 0.95]
            text: "`\\\\mathbf{E[X]} = ${calcs.media.toFixed(2)}`"
            bgcolor: colors.bg
            color: colors.ink
            fontSize: 7
            position: b

        # =========================
        # Segmento de 1 desvio padrão
        # =========================
        - Arrow:
            begin: [calcs.media, 0.88]
            end: [calcs.media + calcs.sigma, 0.88]
            color: colors.soft
            strokeWidth: 1.5
            label:
              text: "`+ ${calcs.sigma.toFixed(2)}`"
              position: t
              fontSize: 6

        - Arrow:
            begin: [calcs.media, 0.88]
            end: [calcs.media - calcs.sigma, 0.88]
            color: colors.soft
            strokeWidth: 1.5
            label:
              text: "`- ${calcs.sigma.toFixed(2)}`"
              position: t
              fontSize: 6

        # =========================
        # Labels do eixo x
        # =========================
        - Label:
            coordinates: [0, 0]
            text: "'0'"
            bgcolor: colors.bg
            color: colors.ink
            fontSize: 7
            position: t

        - Label:
            coordinates: [1, 0]
            text: "'1'"
            bgcolor: colors.bg
            color: colors.ink
            fontSize: 7
            position: t

        # =========================
        # Labels sobre as barras
        # =========================
        - Label:
            coordinates: [0, calcs.p0 + 0.03]
            text: "`${calcs.p0.toFixed(3)}`"
            bgcolor: colors.bg
            color: colors.ink
            fontSize: 7

        - Label:
            coordinates: [1, calcs.p1 + 0.03]
            text: "`${calcs.p1.toFixed(3)}`"
            bgcolor: colors.bg
            color: colors.ink
            fontSize: 7

    sidebar:
      triggerWidth: 9999
      controls:
        - title: 
          sliders:
            - param: p
              label: "p"

          divs:
            # ---------------------------
            # 1) DADOS
            # ---------------------------
            - html: "<div style='text-align:left; font-size:11px; margin:15px 0 15px 0;'><b>Dados:</b></div>"

            - html: "`$$\\\\tiny
                      X\\\\sim Bernoulli(p)
                      $$`"

            - html: "`$$\\\\tiny
                      p=${params.p.toFixed(3)}
                      $$`"

            # ---------------------------
            # 2) FUNÇÃO DE PROBABILIDADE
            # ---------------------------
            - html: "<div style='text-align:left; font-size:11px; margin:15px 0 15px 0;'><b>Probabilidade:</b></div>"

            - html: "`$$\\\\tiny
                      P(X=0)=1-p,\\\\qquad P(X=1)=p
                      $$`"

            - html: "`$$\\\\tiny
                      P(X=0)=${calcs.p0.toFixed(3)},\\\\qquad
                      P(X=1)=${calcs.p1.toFixed(3)}
                      $$`"

            # ---------------------------
            # 3) MÉDIA
            # ---------------------------
            - html: "<div style='text-align:left; font-size:11px; margin:15px 0 15px 0;'><b>Média:</b></div>"

            - html: "`$$\\\\tiny
                      E[X]=p
                      $$`"

            - html: "`$$\\\\tiny
                      E[X]=${calcs.media.toFixed(3)}
                      $$`"

            # ---------------------------
            # 4) DESVIO PADRÃO
            # ---------------------------
            - html: "<div style='text-align:left; font-size:11px; margin:15px 0 15px 0;'><b>Desvio padrão:</b></div>"

            - html: "`$$\\\\tiny
                      \\\\sigma=\\\\sqrt{p(1-p)}
                      $$`"

            - html: "`$$\\\\tiny
                      \\\\sigma=${calcs.sigma.toFixed(3)}
                      $$`"

            # ---------------------------
            # 5) CHECAGEM
            # ---------------------------
            - html: "<div style='text-align:left; font-size:11px; margin:15px 0 15px 0;'><b>Checagem:</b></div>"

            - html: "`$$\\\\tiny
                      P(X=0)+P(X=1)=${calcs.somaProb.toFixed(3)}
                      $$`"
